export default function Background3D() {
  return (
    <div className="fixed inset-0 z-[-1] pointer-events-none bg-black">
      {/* Background is pure black for high-contrast minimalist look */}
    </div>
  );
}
